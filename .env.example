# Free OMDb API key: https://www.omdbapi.com/apikey.aspx
# Copy this file to .env.local and fill in your own key.
#
# No VITE_ prefix on purpose: only the serverless proxy in api/omdb.js reads it,
# so it never reaches the browser. A key bundled into client code is a public key.
OMDB_API_KEY=
